apiVersion: v1
kind: Service
metadata:
  name: mi-web-service
spec:
  selector:
    app: mi-web
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
      nodePort: 30080